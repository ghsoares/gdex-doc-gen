<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>{{class_name}} - {{project_title}}</title>
	{{default_header}}
</head>

<body>
	{{sidebar}}
	<div>
		{{doc_header}}
		<section id="class">
			<h1 link-section>{{class_name}}</h1>
			<p>{{brief_description}}</p>
		</section>
		<section id="description">
			<h2 link-section>Description</h2>
			<p>{{description}}</p>
		</section>
		<section id="methods">
			<h2 link-section>Methods</h2>
			<table id="class-method-table">
				<tbody>
					{{method_reference_items}}
				</tbody>
			</table>
		</section>
		<section id="method-descriptions">
			<h2 link-section>Method Descriptions</h2>
			{{method_description_items}}
		</section>
		{{default_footer}}
	</div>
</body>

</html>